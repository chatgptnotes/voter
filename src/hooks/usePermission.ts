/**
 * Permission Hooks
 * Re-export from PermissionContext for convenience
 */

export {
  usePermissions,
  usePermission,
  useRole,
  useIsSuperAdmin,
  useIsAdmin,
  useIsAdminOrManager,
  useOrganization,
  useFeatureAccess,
} from '../contexts/PermissionContext';
